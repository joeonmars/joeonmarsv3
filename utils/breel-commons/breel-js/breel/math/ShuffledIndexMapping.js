/**
 * @fileoverview Shuffled Index Mapping.
 */

goog.provide('breel.math.ShuffledIndexMapping');

/**
 * Shuffled Index Mapping
 *
 * @constructor
 * @extends {goog.events.EventTarget}
 */
breel.math.ShuffledIndexMapping = function(numDigits) {
  goog.base(this);

  this.numDigits = numDigits;

  
};